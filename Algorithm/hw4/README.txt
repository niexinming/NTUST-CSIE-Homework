# HW4 -- Singly Connected Path

## 概念

從每個 `V` 跑一次 DFS，如果發現兩種或以上的簡單路徑可以走到同一個點，則這個 graph 不是 singly connected graph

## 編譯方式

有 `Makefile`，UNIX 環境下 `make` 即可，輸出執行檔案為 `prog`

## 環境要求

- 需有 `g++` 編譯器
- 版本至少在 4.7 或以上
- 或者使用支援 C++11 標準之編譯器

> vim: ft=markdown
